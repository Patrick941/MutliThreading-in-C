	switch (t->back) {
	default: Uerror("bad return move");
	case  0: goto R999; /* nothing to undo */

		 /* PROC Q */

	case 3: // STATE 1
		;
		n = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 5: // STATE 3
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC P */

	case 6: // STATE 1
		;
		n = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 8: // STATE 3
		;
		p_restor(II);
		;
		;
		goto R999;
	}

